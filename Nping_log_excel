from pyats.easypy import run
import os
import pandas as pd 
import smtplib

def main():
    def run_testfiles(l):
        for i in l:
            print("Running {}".format(i))
            import pdb;pdb.set_trace()
            result = run(i, datafile = 'acl_datafile.yaml', pdb=True)
            import pdb;pdb.set_trace()
            df = pd.DataFrame(result, index = [0])
            with pd.ExcelWriter('Testcase_output.xlsx',mode='a') as writer:  
                df.to_excel(writer, sheet_name='TestSuiteOutput')
            
    l = ['test_case1.py', 'test_case2.py', 'test_case3.py', 'test_case4.py']
    run_testfiles(l)
    '''
    sender_email = 'xyz@tcs.com'
    app_pwd = 'sdsfsdfhgsdhfgjf' #16 character app password which we need to generate in our browser
    receiver_email = 'abcd@tcs.com'
    subject = "testcase status"
    message_text = 'All testcases ran succuessfully.'
    try:
        #connect to SMTP server.
        server = smtplib.SMTP('smtp.office365.com', 587)
        server.startls()
        
        #log in with mail and app password.
        server.sendmail(sender_email, app_pwd)
 
        #Create and send the email
        message = f"Subject: {subject} \n\n{message_text}"
        server.sendmail(sender_email, receiver_email, message)
     
        print("Email sent successfully!")
        server.quit()

    except Exception as e:
        print("An error occurred:", str(e))
    '''     
 
